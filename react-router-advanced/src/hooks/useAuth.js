// Simple hook to simulate authentication
const useAuth = () => {
  const isAuthenticated = false; // replace with real login logic if needed
  return { isAuthenticated };
};

export default useAuth;
